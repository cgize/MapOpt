// Main module - entry point for the application
// Este archivo se encarga de cargar todos los módulos en el orden correcto

// Asegurarse de que el DOM esté cargado antes de inicializar la aplicación
document.addEventListener('DOMContentLoaded', function() {
    console.log('MapOpt: Aplicación iniciada');
    
    // Los módulos específicos (map.js, data.js, ui.js) se inicializarán automáticamente
    // cuando se carguen sus scripts en el HTML
});